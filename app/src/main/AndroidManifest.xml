<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_zerocry">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_ZeroCry">
        <activity android:name=".purpActivity1"></activity>
        <activity android:name=".story" />
        <activity android:name=".regActivity" />
        <activity android:name=".ngo_Register1" />
        <activity android:name=".loginActivity1" />
        <activity android:name=".chatActivity1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewn" />
        <activity android:name=".view2" />
    </application>

</manifest>